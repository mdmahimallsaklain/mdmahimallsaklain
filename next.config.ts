import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["backend-mdmahimallsaklain.vercel.app"],
  },
  // You can add other Next.js configurations here
};

export default nextConfig;
